# Release Notes — v2.1

## 更新日期
2026-05-14

## 新增功能

### 应用图标
- 新增专属应用图标（蓝色渐变盾牌 + 白色闪电）
- 图标尺寸：256×256 PNG + 多尺寸 ICO（Windows 任务栏兼容）

### 文字修改
- 精灵属性选择按钮文字从「+ 第二属性」改为「+ 添加属性」，表述更直观

## Bug 修复

### 修复：结果面板图标随点击次数下移

**问题现象**：连续点击「开始计算」后，结果面板中的属性图标行会不断向下偏移。

**根本原因**：`ResultPanel::updateResults()` 使用 `qDeleteAll(findChildren<QWidget*>())` 清除旧内容，但 `layout->addStretch()` 添加的 `QSpacerItem` 不是 Widget，不会被该方法删除。每次计算都会叠加一个 stretch，导致 layout 高度持续增大。

**修复方案**：改用 `layout->takeAt(0)` 循环，彻底清除所有 layout 条目（包括 Widget 和 Spacer），确保每次重建内容时 layout 为空。

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| v2.1 | 2026-05-14 | Bug 修复 + 图标 + 文字优化 |
| v2.0 | 2026-05-04 | 新增打击盲点计算器（Tab 双模式） |
| v1.1 | 2026-05-01 | 属性图标增大 + 选择窗口优化 |
| v1.0 | 2026-04-30 | 初版发布（联防盲点计算器） |
